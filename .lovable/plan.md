Confirmed with the user: keep the current cookie consent configuration as-is.

- The main consent banner remains centered, blocks page interaction, and shows only 'Zgadzam się' and 'Ustawienia' (the 'Tylko niezbędne' button is removed).
- The advanced preferences modal keeps the 'Nie wyrażam zgody' button, matching the screenshot the user shared.
- No code changes are required.

If any further adjustments are needed, we can revisit in a new request.